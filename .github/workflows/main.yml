name: Build and Deploy Workflow

on:

  push:

    branches:

      - main

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Build step

        run: echo "Build job executed successfully"

  deploy:

    needs: build

    runs-on: ubuntu-latest

    steps:

      - name: Deploy step

        run: echo "Deploy job executed successfully"
